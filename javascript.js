//create variable const computerSelection to hold random computer choice

//create function getComputerChoice() that randomly selects between 0 and 2
//function getComputerChoice() uses number selection to pick between rock, paper or scissors
//function getComputerChoice() puts random selection into variable computerSelection

//create variable let playerSelection()

//create function getPlayerChoice() that takes user input [choice between rock paper or scissors], converts toUpperCase, returns it to result.
    //inside getPlayerChoice(), playerSelection = result

//PLAY ROUND
//create function playRound with parameters playerSelection and computerSelection
//function playRound compares using A CONDITIONAL, holds in local variable const result and provides output to console with who won.
    //playerSelection === computer Selection
        //Tie game, you both picked result
    //playerSelection === 'ROCK' && computerSelection === 'SCISSORS'
    //playerSelection === 'PAPER' && computerSelection === 'ROCK'
    //playerSelection === 'SCISSORS' && computerSelection === 'PAPER'
        //You win! playerSelection beats computerSelection!
    //computerSelection === 'ROCK' && playerSelection === 'SCISSORS'
    //computerSelection === 'PAPER' && playerSelection === 'ROCK'
    //computerSelection === 'SCISSORS' && playerSelection === 'PAPER'
        //You win! playerSelection beats computerSelection!
    //default: Oops. Invalid choice. Please select ROCK, PAPER or SCISSORS
        //getPlayerChoice()

    
